type Team {
  id: ID!
  name: String!
  extra: [String]
}

type Query {
  team(id: ID!): Team
}

input CreateTeamInput {
  name: String!
}

type CreateTeamResponse {
  team: Team!
}
type Mutation {
  createTeam(input: CreateTeamInput!): CreateTeamResponse
}
